import React from 'react'
import { Button, Label } from './styles'

export default function RequestButton({ title }) {
  return (
    <>
      <Label></Label>
      <Button>{title}</Button>
    </>
  )
}
